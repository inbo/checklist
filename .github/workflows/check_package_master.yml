on:
  push:
    branches:
      - master

name: "check package on master"

jobs:
  check-package:
    runs-on: ubuntu-latest
    name: "check package"
    steps:
      - uses: inbo/actions/check_pkg@master
